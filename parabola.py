# Parabola/Parabola.py
import sys
from pathlib import Path

# Add the path to the 'submodules' directory to the Python path
submodules_path = Path(__file__).parent / 'submodules'
sys.path.append(str(submodules_path))
